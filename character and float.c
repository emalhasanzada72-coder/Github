#include <stdio.h>
int main( ) {
    float a, b, zarb;
    char x;
    printf("Enter to float number :\n Enter character name :🙃\n");
    scanf("%f %f", &a, &b);
    scanf(" %c", &x);
    zarb = a * b;
    printf("my zarb number is %f\n   character name is %c\n 😜", zarb, x );
    return 0; 

}