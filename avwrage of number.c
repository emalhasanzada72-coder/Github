#include <stdio.h>

int main() {
    int matrix[2][2];
    int sum = 0;

    printf("Enter 4 numbers for a 2x2 matrix:\n");

    // بخش مربوط به حلقه‌ها را تو بنویس
    // راهنمایی: به دو حلقه for نیاز داری (یکی برای سطر i و یکی برای ستون j)
    
    for (int i = 0; i < 2; i++) {
        for (int j = 0; j < 2; j++) {
            // ۱. از کاربر ورودی بگیر (scanf)
            // ۲. عدد را به sum اضافه کن
        }
    }

    printf("The total sum is: %d\n", sum);

    return 0;
}