
#include <stdio.h>
int main(){
    int f[9] = {5, 6, 3, 2, 8, 7, 9, 9,7,6};
    int sum = 0; 
    int *p = f;
    while (p < f + 9)    {
        sum += *p;
        p++;
    
    }
    printf("sum is %d\n", sum);4️⃣ تمرین خیلی مهم ⭐

این کارها را انجام بده:

آرایه 5تایی

با پوینتر جمع عناصر را حساب کن

بدون استفاده از a[i]

قدم بعدی (وقتی آماده بودی):

➡ پوینتر و تابع
➡ پوینتر و Struct

فقط یکی را انتخاب کن و بگو
}