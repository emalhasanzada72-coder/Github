#include <stdio.h> 
int main() {
    int x, y;
    int sum, pridect, diffrance, remainder;
    sum = x + y;
     pridect = x * y;
     diffrance = x - y;
     remainder = x / y;
     printf(" \t Enter tow number \n");
     scanf("%d %d", &x, &y);
     printf("----- result ------------\n");
     printf(" \tmy sum is %d\n ", sum);
     printf("\tmy pridect is %d\n ", pridect);
     printf(" \tmy diffrance is %d\n", diffrance);
     printf(" \tyour remainder is %d\n", remainder );
     return 0;
}